
#include "stdafx.h"
#include <iostream>
using namespace std;

void getScore(int&);
void calcAverage(int );
int arr[5];
int i;
double z;
int main()
{
	
	getScore(i);
	cout << "The five scores are" << arr[i];
	calcAverage(z);



    return 0;
}

void getScore(int& i)
{
	
	cout << "Please enter your test score" << endl;
	for (int i = 0; i < 5; i++) {
		do {
			cin >> arr[i];
		} while ((arr[i] >= 0) && (arr[i] <= 100));
	}
}


void calcAverage(int a)
{
	for (i = 1; i <= 5; i++) 
	{
		if (arr[0] > arr[i])
			arr[0] = arr[i];
	}
	double average = (arr[1] + arr[2] + arr[3] + arr[4]) / 4;
	cout << "The average of four highest score is " << average;
}

